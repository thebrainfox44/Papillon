import React from "react";
import { WebView } from "react-native-webview";

const loginIUTLannion = (username, password) => {
  const ourWebview = WebView;
  console.log(ourWebview);
};

export default loginIUTLannion;
